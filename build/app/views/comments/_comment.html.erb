<div class="comment-card">
  <div class="comment-header">
    <% if comment.user.avatar.attached? %>
      <%= image_tag url_for(comment.user.avatar), class: "comment-avatar" %>
    <% else %>
      <%= image_tag "Profile_page/default_avatar.svg", class: "comment-avatar", alt: "Avatar" %>
    <% end %>

    <div class="comment-info">
      <p class="comment-meta">
        <strong><%= comment.user.username %></strong>
        · <%= time_ago_in_words(comment.created_at) %> fa
      </p>
    </div>

    <% if comment.user == current_user %>
      <div class="comment-actions">
        <%= button_to 'Cancella', [comment.post, comment], method: :delete, data: { confirm: 'Sei sicuro?' }, class: "btn forum-button2" %>
      </div>
    <% end %>
  </div>

  <div class="comment-body">
    <p><%= comment.content %></p>
  </div>
</div>
