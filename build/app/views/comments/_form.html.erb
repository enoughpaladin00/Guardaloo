<%= form_with(model: [post, post.comments.build]) do |form| %>
  <div class="field">
    <%= form.label :'Scrivi il tuo commento!', class:"post-title" %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<!-- modifica estetica -->